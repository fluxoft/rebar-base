<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title><?= htmlspecialchars($rebarTemplateData['title']) ?></title>
	<style>
		body {
			font-family: Arial, sans-serif;
			margin: 0;
			padding: 0;
			line-height: 1.6;
		}
		header {
			background-color: #0056b3; /* Darker blue for the background */
			color: #f4f4f9; /* Light text for contrast */
			padding: 20px;
			text-align: center;
		}
		header h1 {
			margin: 0;
			font-size: 24px;
		}
		form {
			max-width: 600px;
			margin: 20px auto;
			padding: 20px;
			border: 1px solid #ddd;
			border-radius: 5px;
			background-color: #f9f9f9;
		}
		label {
			display: block;
			margin-bottom: 5px;
			font-weight: bold;
		}
		input {
			width: 100%;
			padding: 8px;
			margin-bottom: 15px;
			border: 1px solid #ccc;
			border-radius: 4px;
			box-sizing: border-box;
		}
		button {
			background-color: #007BFF;
			color: white;
			padding: 10px 15px;
			border: none;
			border-radius: 4px;
			cursor: pointer;
		}
		button:hover {
			background-color: #0056b3;
		}
		.navigation-links {
			text-align: center;
			margin-top: 20px;
		}
		.navigation-links a {
			display: inline-block;
			padding: 10px 15px;
			background-color: #007BFF;
			color: white;
			text-decoration: none;
			border-radius: 5px;
			margin: 5px;
		}
		.navigation-links a:hover {
			background-color: #0056b3;
		}
	</style>
</head>
<body>
	<header>
		<h1><?= htmlspecialchars($rebarTemplateData['title']) ?></h1>
	</header>

	<form action="/materials/save" method="post">
		<?php if (!empty($rebarTemplateData['material']->Id)): ?>
			<input type="hidden" name="Id" value="<?= htmlspecialchars($rebarTemplateData['material']->Id) ?>">
		<?php endif; ?>

		<label for="Name">Name:</label>
		<input type="text" id="Name" name="Name" value="<?= htmlspecialchars($rebarTemplateData['material']->Name ?? '') ?>" required>

		<label for="Quantity">Quantity:</label>
		<input type="number" id="Quantity" name="Quantity" value="<?= htmlspecialchars($rebarTemplateData['material']->Quantity ?? 0) ?>" required>

		<label for="UnitPrice">Unit Price:</label>
		<input type="number" id="UnitPrice" name="UnitPrice" step="0.01" value="<?= htmlspecialchars($rebarTemplateData['material']->UnitPrice ?? 0.0) ?>" required>

		<button type="submit">Save</button>
	</form>

	<div class="navigation-links">
		<a href="/materials">Back to Materials List</a>
	</div>
</body>
</html>
