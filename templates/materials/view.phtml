<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title><?= htmlspecialchars($rebarTemplateData['title'], ENT_QUOTES, 'UTF-8') ?></title>
	<style>
		body {
			font-family: Arial, sans-serif;
			margin: 0;
			padding: 0;
			line-height: 1.6;
		}
		header {
			background-color: #0056b3; /* Darker blue for the background */
			color: #f4f4f9; /* Light text for contrast */
			padding: 20px;
			text-align: center;
		}
		header h1 {
			margin: 0;
			font-size: 24px;
		}
		table {
			width: 100%;
			border-collapse: collapse;
			margin: 20px auto;
			max-width: 600px;
		}
		th, td {
			padding: 10px;
			border: 1px solid #ddd;
			text-align: left;
		}
		th {
			background-color: #f4f4f4;
			width: 30%;
		}
		td.numeric {
			text-align: right;
		}
		.navigation-links {
			text-align: center;
			margin-top: 20px;
		}
		.navigation-links a {
			display: inline-block;
			padding: 10px 15px;
			background-color: #007BFF;
			color: white;
			text-decoration: none;
			border-radius: 5px;
			margin: 5px;
		}
		.navigation-links a:hover {
			background-color: #0056b3;
		}
	</style>
</head>
<body>
	<header>
		<h1><?= htmlspecialchars($rebarTemplateData['title'], ENT_QUOTES, 'UTF-8') ?></h1>
	</header>

	<table>
		<tbody>
			<tr>
				<th>ID</th>
				<td><?= htmlspecialchars($rebarTemplateData['material']->Id, ENT_QUOTES, 'UTF-8') ?></td>
			</tr>
			<tr>
				<th>Name</th>
				<td><?= htmlspecialchars($rebarTemplateData['material']->Name, ENT_QUOTES, 'UTF-8') ?></td>
			</tr>
			<tr>
				<th>Quantity</th>
				<td class="numeric"><?= htmlspecialchars(number_format($rebarTemplateData['material']->Quantity ?? 0), ENT_QUOTES, 'UTF-8') ?></td>
			</tr>
			<tr>
				<th>Unit Price</th>
				<td class="numeric">$<?= htmlspecialchars(number_format($rebarTemplateData['material']->UnitPrice ?? 0, 2), ENT_QUOTES, 'UTF-8') ?></td>
			</tr>
			<tr>
				<th>Total Value</th>
				<td class="numeric">$<?= htmlspecialchars(number_format(($rebarTemplateData['material']->Quantity ?? 0) * ($rebarTemplateData['material']->UnitPrice ?? 0), 2), ENT_QUOTES, 'UTF-8') ?></td>
			</tr>
		</tbody>
	</table>

	<div class="navigation-links">
		<a href="/materials">Back to Materials List</a>
	</div>
</body>
</html>
