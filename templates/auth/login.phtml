<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title><?= htmlspecialchars($rebarTemplateData['title'] ?? 'Login') ?></title>
	<style>
		body {
			font-family: Arial, sans-serif;
			margin: 0;
			padding: 0;
			line-height: 1.6;
		}
		header {
			background-color: #0056b3; /* Darker blue for the background */
			color: #f4f4f9; /* Light text for contrast */
			padding: 20px;
			text-align: center;
		}
		header h1 {
			margin: 0;
			font-size: 24px;
		}
		form {
			max-width: 400px;
			margin: 20px auto;
			padding: 20px;
			border: 1px solid #ddd;
			border-radius: 5px;
			background-color: #f9f9f9;
		}
		label {
			display: block;
			margin-bottom: 5px;
			font-weight: bold;
		}
		input {
			width: 100%;
			padding: 8px;
			margin-bottom: 15px;
			border: 1px solid #ccc;
			border-radius: 4px;
			box-sizing: border-box;
		}
		button {
			background-color: #007BFF;
			color: white;
			padding: 10px 15px;
			border: none;
			border-radius: 4px;
			cursor: pointer;
		}
		button:hover {
			background-color: #0056b3;
		}
		.error {
			color: red;
			margin-bottom: 15px;
		}
		.note {
			text-align: center;
			margin: 20px auto;
			padding: 10px;
			background-color: #e9f7ef; /* Light green background */
			color: #3c763d; /* Dark green text */
			border: 1px solid #d6e9c6;
			border-radius: 5px;
			max-width: 400px;
		}
	</style>
</head>
<body>
	<header>
		<h1><?= htmlspecialchars($rebarTemplateData['title'] ?? 'Login') ?></h1>
	</header>

	<div class="note">
		<p>Use the default username and password:</p>
		<p><strong>Username:</strong> testuser</p>
		<p><strong>Password:</strong> testpassword</p>
	</div>

	<?php if (!empty($rebarTemplateData['error'] ?? '')): ?>
		<div class="error">
			<p><?= htmlspecialchars($rebarTemplateData['error']) ?></p>
		</div>
	<?php endif; ?>

	<form method="POST" action="/auth/login">
		<div>
			<label for="username">Username:</label>
			<input
				type="text"
				id="username"
				name="username"
				value="<?= htmlspecialchars($rebarTemplateData['username'] ?? '') ?>"
				required>
		</div>

		<div>
			<label for="password">Password:</label>
			<input type="password" id="password" name="password" required>
		</div>

		<input type="hidden" name="redirect" value="<?= htmlspecialchars($rebarTemplateData['redirect'] ?? '/') ?>">

		<div>
			<button type="submit">Login</button>
		</div>
	</form>
</body>
</html>
